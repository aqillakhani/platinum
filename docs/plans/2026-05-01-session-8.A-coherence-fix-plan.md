# S8.A — Wan I2V coherence fix (plan)

**Date:** 2026-05-01
**Author:** Claude
**Design:** `2026-05-01-session-8.A-coherence-fix-design.md`

---

## Files

### Modified
- `config/tracks/atmospheric_horror.yaml` — swap `video_model.width`/`height` to 544/960.
- `config/workflows/wan22_i2v.json` — sampler cfg/steps; expand negative prompt.
- `src/platinum/models/story.py` — add `Scene.motion_prompt: str | None`; round-trip.
- `src/platinum/pipeline/video_generator.py` — prefer `scene.motion_prompt` over `scene.visual_prompt`.
- `src/platinum/cli.py` — register `platinum motion-prompts <story_id>` command.
- `src/platinum/pipeline/orchestrator.py` — insert `motion_prompts` between `keyframe_review` and `video_generator` in `CANONICAL_STAGE_NAMES`.

### New
- `src/platinum/pipeline/motion_prompts.py` — stage + helper functions.
- `config/prompts/atmospheric_horror/motion_prompt.j2` — Jinja2 motion-prompt template.
- `config/prompts/folktales_world_myths/motion_prompt.j2` — same content (copy).
- `config/prompts/childrens_fables/motion_prompt.j2` — same content (copy).
- `config/prompts/scifi_concept/motion_prompt.j2` — same content (copy).
- `config/prompts/slice_of_life/motion_prompt.j2` — same content (copy).
- `tests/unit/test_motion_prompts.py` — request building, response parsing, scene mutation.
- `tests/integration/test_motion_prompts_stage.py` — Stage with Recorder, full round-trip.
- `tests/integration/test_track_aspect_consistency.py` — image_model vs video_model orientation match.

---

## TDD checklist

### S8.A.1 Aspect fix
- [ ] `test_track_yaml_video_aspect_matches_image_aspect` — atmospheric_horror.yaml: image_model 768x1344 (portrait) → video_model 544x960 (portrait). Aspect within 10%.
- [ ] Same for any other track that declares video_model dims (currently only atmospheric_horror does).
- [ ] Existing aspect-related tests still pass.
- [ ] `ruff check` clean; `mypy` clean.

### S8.A.2 Sampler + neg prompt
- [ ] Find any test that asserts cfg=6.0, steps=20, end_step=10. Update the asserted values.
- [ ] `test_wan_workflow_json_negative_prompt_contains_coherence_terms` — assert duplicate/reverse/morph terms present.
- [ ] `_check_wan_workflow_json` (preflight) still validates the new shape.

### S8.A.3 Scene.motion_prompt
- [ ] `test_scene_motion_prompt_round_trip` — set field, save, load, equal.
- [ ] `test_scene_motion_prompt_defaults_none_when_missing` — load old JSON without the field; default None.

### S8.A.4 motion_prompts stage (unit)
- [ ] `test_build_request_includes_keyframe_image` — image base64 block present in messages.
- [ ] `test_build_request_includes_visual_prompt_context` — visual_prompt is in the prompt body (so Claude knows the intended action).
- [ ] `test_build_request_renders_template_for_track` — template lookup uses story.track.
- [ ] `test_zip_response_into_scene_mutates_motion_prompt` — sets scene.motion_prompt.
- [ ] `test_zip_response_skips_scenes_without_keyframe_path` — no keyframe = no Claude call.
- [ ] `test_motion_prompts_skips_already_set_scenes` — idempotent re-run.
- [ ] `test_motion_prompts_protocol_error_on_missing_field` — raises if response lacks motion_prompt.

### S8.A.5 motion_prompts stage (integration)
- [ ] `test_motion_prompts_stage_runs_with_recorder` — full Stage.run() with fake Recorder, verify scenes mutated, story.json persisted.
- [ ] `test_motion_prompts_stage_skips_when_no_approved_scenes` — empty approved list = no-op, no Claude calls.
- [ ] `test_motion_prompts_stage_records_usage` — cost_usd plumbed into stage artifacts.

### S8.A.6 video_generator integration
- [ ] `test_video_generator_uses_motion_prompt_when_present` — workflow injection sees motion_prompt.
- [ ] `test_video_generator_falls_back_to_visual_prompt` — when motion_prompt is None, visual_prompt is used.

### S8.A.7 CLI
- [ ] `test_cli_motion_prompts_command_exists` — `platinum motion-prompts --help` returns 0.
- [ ] `test_cli_motion_prompts_runs_stage` — invoke against fixture story, verify motion_prompt set.

### S8.A.8 Orchestrator wiring
- [ ] `test_canonical_stage_order_includes_motion_prompts_between_review_and_video` — list contains the right neighbour ordering.

---

## Quality gates

- All existing tests pass (current count to be checked locally; expecting 547+).
- New tests >= 80% coverage on motion_prompts.py.
- `ruff check src tests` clean.
- `mypy src` clean.
- `bash -n scripts/vast_setup.sh` clean.
- Manual: `git status` clean before commit.

---

## Commit plan

One logical commit per S8.A.X subsection:

1. `feat(s8.A.1): swap atmospheric_horror video_model to 544x960 portrait`
2. `feat(s8.A.2): tune Wan I2V cfg=2.5/steps=10 + coherence negative prompt`
3. `feat(s8.A.3): add Scene.motion_prompt field with JSON round-trip`
4. `feat(s8.A.4): motion_prompts stage core (helpers, request/response)`
5. `feat(s8.A.5): MotionPromptsStage + integration tests`
6. `feat(s8.A.6): video_generator prefers scene.motion_prompt`
7. `feat(s8.A.7): platinum motion-prompts CLI command`
8. `feat(s8.A.8): wire motion_prompts into canonical stage order`
9. `docs(s8.A): per-track motion_prompt.j2 templates`
10. `feat(s8.A.10): bump memory to S8.A coherence fix`

Push as a single 10-commit stack.

---

## Out of scope

- FLF2V (first-last-frame conditioning) — research-grade gain but doubles
  keyframe authoring cost; revisit if (1)+(2)+(3)+(4) isn't enough after the next render.
- Motion-LoRA adapters — experimental for Wan 2.2; nothing production-ready.
- Per-scene-class motion_min_flow recalibration — separate S8.A.11 task,
  needs distribution data from re-render.
- Splitting `i2v_fps` (16) from `publish_fps` (24) — separate S8.A.12.
- S9 upscale (RealESRGAN 540p → 1080p).
